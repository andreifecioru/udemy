const todos = ['eat', 'sleep', 'play', 'work', 'watch tv']

// Delete 3rd item
todos.splice(2, 1)

// Add a new item @ the end
todos.push('go outside')

// Remove 1st item
todos.shift()

console.log(todos)